ShaderType = Fragment;

Input fsIn{
    float4 position: SRV_POSITION;
    float3 normal: Normal;
    float2 texCoords: TexCoords;
};

Output fsOut{
    float4 color: SRV_TARGET0;
};

void main(){
    fsOut.color = float4(0.2f, 0.3f, 0.8f, 1.0f);
}