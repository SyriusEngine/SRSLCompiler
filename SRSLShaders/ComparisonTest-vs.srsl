ShaderType = Vertex;

Input vsIn{
    float3 position: Position;
    float3 normal: Normal;
    float2 texCoords: TexCoords;
};

Output vsOut{
    float4 position: SRV_POSITION;
    float3 normal: Normal;
    float2 texCoords: TexCoords;
};

void main(){
    float f1 = 1.0;
    float f2 = 2.0;

    bool fEq = f1 == f2;

    uint4 uv1 = uint4(1, 2, 3, 4);
    uint4 uv2 = uint4(5, 6, 3, 4);

    bool uvEq = uv1 == uv2;
}